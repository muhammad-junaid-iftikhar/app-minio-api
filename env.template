# Application Configuration
APP_ENV=development
SERVER_PORT=8080

# Volume paths - Uncomment the appropriate section for your OS

############## MAC OS ##############
# MinIO
# USER_PROFILE=m.iftikhar
# volume_minio_path=/Users/${USER_PROFILE}/docker_volumes/minio/minio_data
# vector_volume_path=/Users/${USER_PROFILE}/docker_volumes/minio/vector

############## LINUX ##############
# Uncomment these lines for Linux and comment out the Mac section above
USER_PROFILE=juni
volume_minio_path=/home/${USER_PROFILE}/docker_volumes/minio/minio_data
vector_volume_path=/home/${USER_PROFILE}/docker_volumes/minio/vector

# MinIO Configuration
MINIO_ROOT_USER=adminuser
MINIO_ROOT_PASSWORD=sdfs235vww2@235235
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_USE_SSL=false
MINIO_BUCKET_NAME=api-uploads
MINIO_ENDPOINT=localhost

# Cloudflare R2 Configuration
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key
R2_PUBLIC_URL=your_r2_public_url  # e.g., https://pub-<account-id>.r2.dev
R2_REGION=auto  # Cloudflare R2 uses 'auto' as the region

# Logging
PROJECT_ID=drive-juni-studio-prod
APP_NAME=app-minio-api
APP_SOURCE=prod

# API Configuration
# For development
# API_BASE_URL=localhost:8080

# For production (uncomment and update this line)
# API_BASE_URL=your-production-domain.com

# Presigned URL Configuration
PRESIGNED_URL_EXPIRY=3600  # 1 hour in seconds
MAX_FILE_SIZE=104857600  # 100MB in bytes

# Authentication Service Configuration
AUTH_SERVICE_URL=http://localhost:8081  

